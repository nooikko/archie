# Run type checking
echo "Running type check..."
pnpm typecheck || exit 1

# Run biome check (linting and formatting)
echo "Running biome check..."
pnpm lint:check || exit 1

# Run tests
echo "Running tests..."
pnpm test || exit 1

echo "Pre-commit checks passed!"
